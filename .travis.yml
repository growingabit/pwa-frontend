language: node_js
sudo: false
before_script:
    - npm install -g bower web-component-tester
    - bower install
node_js: stable
addons:
    firefox: latest
    apt:
        sources:
            - google-chrome
        packages:
            - google-chrome-stable
script:
    - xvfb-run wct
